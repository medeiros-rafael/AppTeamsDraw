import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    image: {
        height: 70,
        width: 70,
        marginTop: 50,
        marginBottom: 30,
        tintColor: '#fff' 
    },
    text: {
        color: '#fff',
        fontSize: 30,
        fontWeight: 'bold',
        marginBottom: 80
    }
});